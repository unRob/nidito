description: Bridge between 1Password public's API and our local network
docs:
  - https://developer.1password.com/docs/connect
packages:
  api:
    # this is incomplete, -api/-sync is added in nomadfile
    image: 1password/connect
    source: https://github.com/1Password/connect
    check: github-changelog
    version: "1.7.3"
    comparison: suffix
