summary: bootstraps the nidito repo
description: |
  cloning it out
arguments:
  - name: repo
    description: the repo source to clone
    default: https://github.com/unRob/nidito.git
  - name: branch
    default: main
    description: the branch to clone from
