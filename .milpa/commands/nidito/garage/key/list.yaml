# see `milpa help docs command spec` for all the options
summary: lists keys
description: |
  from the API, optionally ignoring ids
options:
  name-only:
    description: return name of keys only, not ids
    type: bool
