summary: lists datacenter names
description: |
  from config or consul
options:
  source:
    default: local
    description: where to get values from
    values:
      static: [local, consul]
  format:
    default: text
    description: the format to output the list in
    values:
      static: [text, json]
