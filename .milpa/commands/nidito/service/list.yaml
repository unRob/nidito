summary: List services
description: |
  Lists nidito services, local ones (as defined by `.spec.yaml` files) by default or nomad ones.
options:
  remote:
    type: bool
    description: List remote services instead of local ones
