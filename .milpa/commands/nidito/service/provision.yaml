summary: Provisions infra for a service
description: |
  Runs terraform plan if needed
arguments:
  - name: service
    description: The name of the service to build
    values:
      milpa: nidito service list
options:
  dc:
    description: the dc to switch into
