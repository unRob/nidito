summary: List services
description: |
  Lists nidito services, local ones (as defined by nomad services under /services) by default.
options:
  remote:
    type: boolean
    description: List remote services instead of local ones
